twig:
    file_name_pattern: '*.twig'
    # Use Bootstrap 5 form theme so Symfony form helpers generate Bootstrap-compatible markup
    form_themes: ['bootstrap_5_layout.html.twig']

when@test:
    twig:
        strict_variables: true
